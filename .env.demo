# Demo build: the backend is stubbed in-page (src/demo/backend.ts), so these
# are placeholders. The client refuses to start without them.
VITE_DEMO=1
VITE_SUPABASE_URL=https://demo.invalid
VITE_SUPABASE_ANON_KEY=demo-not-a-real-key
VITE_CURRENCY=R
VITE_RECEIPT_WIDTH=48
