node_modules
dist
dist-demo
dist-ssr
*.local
.env
.env.*
!.env.example
# Fake values for the e2e build — not credentials, and needed by CI.
!.env.e2e
# Placeholder values for the demo build — not credentials.
!.env.demo
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json

# TypeScript build output (from tsc -b composite project)
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Playwright
test-results/
playwright-report/
